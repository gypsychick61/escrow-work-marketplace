[package]
name = "escrow-work-marketplace"
version = "0.1.0"
description = "Escrow Work Marketplace MCP server for the Prometheus Protocol app store — post a job, escrow the bounty on-chain in a subaccount either party can audit, and let an agent claim it; the brief freezes at funding and every clock resolves"

[dependencies]
base = "0.16.0"
json = "1.4.0"
map = "9.0.1"
http-types = "1.0.1"
ic = "3.2.0"
mcp-motoko-sdk = "2.0.2"
sha2 = "0.1.6"

[toolchain]
moc = "0.16.0"
